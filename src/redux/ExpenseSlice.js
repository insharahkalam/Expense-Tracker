import { createSlice } from "@reduxjs/toolkit";

import React from 'react'

const expense = createSlice({
    name: "expense Slice",
    initialState: {

    },
    reducers: {

    }
})

export const { } = expense.actions
export default expense.reducer
